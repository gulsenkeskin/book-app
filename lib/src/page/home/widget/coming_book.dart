import 'package:flutter/material.dart';

class ComingBook extends StatelessWidget {
  const ComingBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Coming Boook');
  }
}
